## Harness

- 所有可拼接地块的视觉必须清晰区分空地、可种植土地和水流；水流保持窄而清晰，优先留出可用土地空间。
- 可种植土地应具有独立的视觉生长状态：未种植时显示裸土，种植后显示覆盖土地的植物，缺水死亡时能够切换为枯萎状态。
- 后续播种与扩张使用的植物只能布置在可种植 `LAND` 的种植区域内；`MEADOW` 上的环境装饰不得充当可播种植物或其生长状态表现。
- 花、草、树等植物预制件应预留统一、可替换颜色的标志性归属部件，用于区分玩家；植物种类及其生长、枯萎状态仍须主要通过轮廓、覆盖范围和中性色细节辨识，不能仅靠整株染色传达。
- 地块边缘采用卡卡颂式端口语法：水流只能从边的中心点连接；土地一旦占边，必须覆盖整条边。土地按占用 1、2、3、4 条边作为不同的地块类型建模，不能用任意内部土块替代边缘类型。
- 每条水流必须视觉上和规则上连到至少一个土地区域，表示真实供水；不得留下只通往空地的孤立水段。多水口地块可由中心主水道分出短支流灌入相邻土地。
- 实际游戏地块的视觉使用固定 `.tscn` 预制件；静态地块定义只选择预制件并提供规则边口，运行时只允许实例化、定位、保持比例缩放到棋盘／手牌容器和 90° 旋转，不能按边口数据重新绘制或拼装地块外观。预制件须可独立编辑其美术而不改变放置规则。
- 参数化地块生成以 3D 预制件为目标：编辑／构建期根据端口、土地连通区和水路烘焙完整 3D 场景、静态网格、拓扑资源及水面 `d/s` 数据；2D 拼装不得作为该能力的替代，运行时仍只实例化生成结果。
- 3D 地块的水面浮沫采用两层结构：静态河道网格须烘焙最近岸线距离 `d` 与岸线弧长坐标 `s`；连续、稳定的纯白基础边缘线的局部波浪宽度、泡团出生时刻和泡团自岸线向水中短距离泛出的中心位置都必须由同一波相 `p(s,t)` 推导，不能分别用独立世界坐标图案生成。河道中央必须保留清水。所有泡沫保持纯白二值遮罩，禁止灰阶、透明度或光照渐变；随机噪声只能控制少量源点的分布、半径和轻微变化，不得造成尖锐锯齿轮廓。当前默认基准为基础线宽度 `0.06`、岸线起伏 `0.5`、波浪频率 `10.0`、浮沫带宽度 `0.05`、泡沫疏密 `10.7`、泡沫半径 `0.4`、主阈值 `0.6`、波动流速 `0.025`；新水面材质与调试面板的“恢复默认”均须采用这组值。
